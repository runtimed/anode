{
  "name": "@anode/docworker",
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "build": "echo 'CloudFlare Worker - no build needed'",
    "clean": "echo 'CloudFlare Worker - no clean needed'",
    "type-check": "echo 'CloudFlare Worker - type checking handled by wrangler'",
    "lint": "echo 'CloudFlare Worker - linting handled by wrangler'"
  },
  "dependencies": {
    "@anode/schema": "workspace:*",
    "@livestore/sync-cf": "catalog:"
  },
  "devDependencies": {
    "wrangler": "catalog:"
  }
}
