// Minimal setup file for schema package tests
// This file is required by the vitest configuration but doesn't need any special setup
// for the schema tests since they are self-contained unit tests

// Export empty object to satisfy module requirements
export {}
