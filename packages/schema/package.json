{
  "name": "@anode/schema",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc --skipLibCheck",
    "dev": "tsc --watch --skipLibCheck",
    "watch": "tsc --watch --skipLibCheck",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "validate-schema": "tsx src/build-time-validation.ts"
  },
  "dependencies": {
    "@livestore/livestore": "catalog:",
    "@effect/schema": "catalog:",
    "effect": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:",
    "@effect/vitest": "catalog:",
    "tsx": "catalog:",
    "@types/node": "catalog:"
  }
}
