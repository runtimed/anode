# Anode Environment Configuration
# Copy this file to .env and update the values for your environment

# Web Client Configuration (VITE_ variables are exposed to the browser)
VITE_LIVESTORE_SYNC_URL=ws://localhost:8787/api
VITE_AUTH_TOKEN=insecure-token-change-me

# Python Runtime Configuration (for @runt packages)
NOTEBOOK_ID=your-notebook-id-from-ui
LIVESTORE_SYNC_URL=ws://localhost:8787/api
AUTH_TOKEN=insecure-token-change-me

# Optional: Google OAuth Configuration for production
# VITE_GOOGLE_CLIENT_ID=your-google-client-id

# Development Notes:
# - For local development, use the default values above
# - For production, point VITE_LIVESTORE_SYNC_URL to your deployed worker
# - Change VITE_AUTH_TOKEN to a secure token matching your worker configuration
# - Set NOTEBOOK_ID from the notebook UI when starting runtime
# - Python runtime and AI features are handled by separate @runt packages
